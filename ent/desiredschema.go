// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/yonidavidson/entsideeffecthooksexample/ent/desiredschema"
)

// DesiredSchema is the model entity for the DesiredSchema schema.
type DesiredSchema struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Alias holds the value of the "alias" field.
	Alias string `json:"alias,omitempty"`
	// Value holds the value of the "value" field.
	Value string `json:"value,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DesiredSchema) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case desiredschema.FieldID:
			values[i] = new(sql.NullInt64)
		case desiredschema.FieldAlias, desiredschema.FieldValue:
			values[i] = new(sql.NullString)
		default:
			return nil, fmt.Errorf("unexpected column %q for type DesiredSchema", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DesiredSchema fields.
func (ds *DesiredSchema) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case desiredschema.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ds.ID = int(value.Int64)
		case desiredschema.FieldAlias:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field alias", values[i])
			} else if value.Valid {
				ds.Alias = value.String
			}
		case desiredschema.FieldValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				ds.Value = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this DesiredSchema.
// Note that you need to call DesiredSchema.Unwrap() before calling this method if this DesiredSchema
// was returned from a transaction, and the transaction was committed or rolled back.
func (ds *DesiredSchema) Update() *DesiredSchemaUpdateOne {
	return (&DesiredSchemaClient{config: ds.config}).UpdateOne(ds)
}

// Unwrap unwraps the DesiredSchema entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ds *DesiredSchema) Unwrap() *DesiredSchema {
	tx, ok := ds.config.driver.(*txDriver)
	if !ok {
		panic("ent: DesiredSchema is not a transactional entity")
	}
	ds.config.driver = tx.drv
	return ds
}

// String implements the fmt.Stringer.
func (ds *DesiredSchema) String() string {
	var builder strings.Builder
	builder.WriteString("DesiredSchema(")
	builder.WriteString(fmt.Sprintf("id=%v", ds.ID))
	builder.WriteString(", alias=")
	builder.WriteString(ds.Alias)
	builder.WriteString(", value=")
	builder.WriteString(ds.Value)
	builder.WriteByte(')')
	return builder.String()
}

// DesiredSchemas is a parsable slice of DesiredSchema.
type DesiredSchemas []*DesiredSchema

func (ds DesiredSchemas) config(cfg config) {
	for _i := range ds {
		ds[_i].config = cfg
	}
}
