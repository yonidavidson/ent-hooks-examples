// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yonidavidson/entsideeffecthooksexample/ent/desiredschema"
	"github.com/yonidavidson/entsideeffecthooksexample/ent/predicate"
)

// DesiredSchemaQuery is the builder for querying DesiredSchema entities.
type DesiredSchemaQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.DesiredSchema
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DesiredSchemaQuery builder.
func (dsq *DesiredSchemaQuery) Where(ps ...predicate.DesiredSchema) *DesiredSchemaQuery {
	dsq.predicates = append(dsq.predicates, ps...)
	return dsq
}

// Limit adds a limit step to the query.
func (dsq *DesiredSchemaQuery) Limit(limit int) *DesiredSchemaQuery {
	dsq.limit = &limit
	return dsq
}

// Offset adds an offset step to the query.
func (dsq *DesiredSchemaQuery) Offset(offset int) *DesiredSchemaQuery {
	dsq.offset = &offset
	return dsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dsq *DesiredSchemaQuery) Unique(unique bool) *DesiredSchemaQuery {
	dsq.unique = &unique
	return dsq
}

// Order adds an order step to the query.
func (dsq *DesiredSchemaQuery) Order(o ...OrderFunc) *DesiredSchemaQuery {
	dsq.order = append(dsq.order, o...)
	return dsq
}

// First returns the first DesiredSchema entity from the query.
// Returns a *NotFoundError when no DesiredSchema was found.
func (dsq *DesiredSchemaQuery) First(ctx context.Context) (*DesiredSchema, error) {
	nodes, err := dsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{desiredschema.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) FirstX(ctx context.Context) *DesiredSchema {
	node, err := dsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DesiredSchema ID from the query.
// Returns a *NotFoundError when no DesiredSchema ID was found.
func (dsq *DesiredSchemaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{desiredschema.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) FirstIDX(ctx context.Context) int {
	id, err := dsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DesiredSchema entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DesiredSchema entity is found.
// Returns a *NotFoundError when no DesiredSchema entities are found.
func (dsq *DesiredSchemaQuery) Only(ctx context.Context) (*DesiredSchema, error) {
	nodes, err := dsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{desiredschema.Label}
	default:
		return nil, &NotSingularError{desiredschema.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) OnlyX(ctx context.Context) *DesiredSchema {
	node, err := dsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DesiredSchema ID in the query.
// Returns a *NotSingularError when more than one DesiredSchema ID is found.
// Returns a *NotFoundError when no entities are found.
func (dsq *DesiredSchemaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = &NotSingularError{desiredschema.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) OnlyIDX(ctx context.Context) int {
	id, err := dsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DesiredSchemas.
func (dsq *DesiredSchemaQuery) All(ctx context.Context) ([]*DesiredSchema, error) {
	if err := dsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return dsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) AllX(ctx context.Context) []*DesiredSchema {
	nodes, err := dsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DesiredSchema IDs.
func (dsq *DesiredSchemaQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := dsq.Select(desiredschema.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) IDsX(ctx context.Context) []int {
	ids, err := dsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dsq *DesiredSchemaQuery) Count(ctx context.Context) (int, error) {
	if err := dsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return dsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) CountX(ctx context.Context) int {
	count, err := dsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dsq *DesiredSchemaQuery) Exist(ctx context.Context) (bool, error) {
	if err := dsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return dsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (dsq *DesiredSchemaQuery) ExistX(ctx context.Context) bool {
	exist, err := dsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DesiredSchemaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dsq *DesiredSchemaQuery) Clone() *DesiredSchemaQuery {
	if dsq == nil {
		return nil
	}
	return &DesiredSchemaQuery{
		config:     dsq.config,
		limit:      dsq.limit,
		offset:     dsq.offset,
		order:      append([]OrderFunc{}, dsq.order...),
		predicates: append([]predicate.DesiredSchema{}, dsq.predicates...),
		// clone intermediate query.
		sql:    dsq.sql.Clone(),
		path:   dsq.path,
		unique: dsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Alias string `json:"alias,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DesiredSchema.Query().
//		GroupBy(desiredschema.FieldAlias).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (dsq *DesiredSchemaQuery) GroupBy(field string, fields ...string) *DesiredSchemaGroupBy {
	group := &DesiredSchemaGroupBy{config: dsq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := dsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return dsq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Alias string `json:"alias,omitempty"`
//	}
//
//	client.DesiredSchema.Query().
//		Select(desiredschema.FieldAlias).
//		Scan(ctx, &v)
//
func (dsq *DesiredSchemaQuery) Select(fields ...string) *DesiredSchemaSelect {
	dsq.fields = append(dsq.fields, fields...)
	return &DesiredSchemaSelect{DesiredSchemaQuery: dsq}
}

func (dsq *DesiredSchemaQuery) prepareQuery(ctx context.Context) error {
	for _, f := range dsq.fields {
		if !desiredschema.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dsq.path != nil {
		prev, err := dsq.path(ctx)
		if err != nil {
			return err
		}
		dsq.sql = prev
	}
	return nil
}

func (dsq *DesiredSchemaQuery) sqlAll(ctx context.Context) ([]*DesiredSchema, error) {
	var (
		nodes = []*DesiredSchema{}
		_spec = dsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &DesiredSchema{config: dsq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		return node.assignValues(columns, values)
	}
	if err := sqlgraph.QueryNodes(ctx, dsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dsq *DesiredSchemaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dsq.querySpec()
	_spec.Node.Columns = dsq.fields
	if len(dsq.fields) > 0 {
		_spec.Unique = dsq.unique != nil && *dsq.unique
	}
	return sqlgraph.CountNodes(ctx, dsq.driver, _spec)
}

func (dsq *DesiredSchemaQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := dsq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (dsq *DesiredSchemaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   desiredschema.Table,
			Columns: desiredschema.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: desiredschema.FieldID,
			},
		},
		From:   dsq.sql,
		Unique: true,
	}
	if unique := dsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := dsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, desiredschema.FieldID)
		for i := range fields {
			if fields[i] != desiredschema.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dsq *DesiredSchemaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dsq.driver.Dialect())
	t1 := builder.Table(desiredschema.Table)
	columns := dsq.fields
	if len(columns) == 0 {
		columns = desiredschema.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dsq.sql != nil {
		selector = dsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dsq.unique != nil && *dsq.unique {
		selector.Distinct()
	}
	for _, p := range dsq.predicates {
		p(selector)
	}
	for _, p := range dsq.order {
		p(selector)
	}
	if offset := dsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DesiredSchemaGroupBy is the group-by builder for DesiredSchema entities.
type DesiredSchemaGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dsgb *DesiredSchemaGroupBy) Aggregate(fns ...AggregateFunc) *DesiredSchemaGroupBy {
	dsgb.fns = append(dsgb.fns, fns...)
	return dsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (dsgb *DesiredSchemaGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := dsgb.path(ctx)
	if err != nil {
		return err
	}
	dsgb.sql = query
	return dsgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := dsgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(dsgb.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := dsgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) StringsX(ctx context.Context) []string {
	v, err := dsgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = dsgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) StringX(ctx context.Context) string {
	v, err := dsgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(dsgb.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := dsgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) IntsX(ctx context.Context) []int {
	v, err := dsgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = dsgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) IntX(ctx context.Context) int {
	v, err := dsgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(dsgb.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := dsgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := dsgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = dsgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) Float64X(ctx context.Context) float64 {
	v, err := dsgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(dsgb.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := dsgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := dsgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (dsgb *DesiredSchemaGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = dsgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (dsgb *DesiredSchemaGroupBy) BoolX(ctx context.Context) bool {
	v, err := dsgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (dsgb *DesiredSchemaGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range dsgb.fields {
		if !desiredschema.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := dsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (dsgb *DesiredSchemaGroupBy) sqlQuery() *sql.Selector {
	selector := dsgb.sql.Select()
	aggregation := make([]string, 0, len(dsgb.fns))
	for _, fn := range dsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(dsgb.fields)+len(dsgb.fns))
		for _, f := range dsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(dsgb.fields...)...)
}

// DesiredSchemaSelect is the builder for selecting fields of DesiredSchema entities.
type DesiredSchemaSelect struct {
	*DesiredSchemaQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (dss *DesiredSchemaSelect) Scan(ctx context.Context, v interface{}) error {
	if err := dss.prepareQuery(ctx); err != nil {
		return err
	}
	dss.sql = dss.DesiredSchemaQuery.sqlQuery(ctx)
	return dss.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (dss *DesiredSchemaSelect) ScanX(ctx context.Context, v interface{}) {
	if err := dss.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) Strings(ctx context.Context) ([]string, error) {
	if len(dss.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := dss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (dss *DesiredSchemaSelect) StringsX(ctx context.Context) []string {
	v, err := dss.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = dss.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (dss *DesiredSchemaSelect) StringX(ctx context.Context) string {
	v, err := dss.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) Ints(ctx context.Context) ([]int, error) {
	if len(dss.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := dss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (dss *DesiredSchemaSelect) IntsX(ctx context.Context) []int {
	v, err := dss.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = dss.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (dss *DesiredSchemaSelect) IntX(ctx context.Context) int {
	v, err := dss.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(dss.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := dss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (dss *DesiredSchemaSelect) Float64sX(ctx context.Context) []float64 {
	v, err := dss.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = dss.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (dss *DesiredSchemaSelect) Float64X(ctx context.Context) float64 {
	v, err := dss.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(dss.fields) > 1 {
		return nil, errors.New("ent: DesiredSchemaSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := dss.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (dss *DesiredSchemaSelect) BoolsX(ctx context.Context) []bool {
	v, err := dss.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (dss *DesiredSchemaSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = dss.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{desiredschema.Label}
	default:
		err = fmt.Errorf("ent: DesiredSchemaSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (dss *DesiredSchemaSelect) BoolX(ctx context.Context) bool {
	v, err := dss.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (dss *DesiredSchemaSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := dss.sql.Query()
	if err := dss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
